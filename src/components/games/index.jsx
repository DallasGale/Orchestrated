import React from 'react'
import PropTypes from 'prop-types'

import Game from '../game'

const Games = () => {
  return (
    <div>
      {/* To .map() over data */}
      <Game />
    </div>
  )
}

export default Games
